//optional property:
const school: {
  name: string;
  established: number;
  location: "Naval2"; //literals
  isRunning: boolean;
  student?: number;
} = {
  name: "NPSN",
  established: 2003,
  location: "Naval2",
  isRunning: true,
  //   student: 500,
};

console.log(school);

//literals: constant value that can not be changed.
